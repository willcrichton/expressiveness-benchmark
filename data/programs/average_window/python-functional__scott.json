{"task": "average_window", "language": "python-functional", "plan": {}, "source": "def average_window(data):\n    def window(value):\n        return math.floor(value[\"time\"] / 7)\n    data = sorted(data, key=lambda d: d[\"time\"])\n    grouped_values = [\n        [point[\"x\"] for point in data if window(point) == w]\n        for w in set(map(window, data))]\n    return [sum(values) / len(values) for values in grouped_values]", "author": "scott", "implementation": ""}